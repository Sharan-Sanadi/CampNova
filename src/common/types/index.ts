/**
 * Shared type definitions re-exported from the data layer.
 * Import from `@/common/types` for convenience.
 */
export type {
  Resource,
  ResourceType,
  ResourceStatus,
  Booking,
  BookingStatus,
  CampusInsight,
  ActivityEvent,
  NotificationItem,
} from "@/data/campus";
